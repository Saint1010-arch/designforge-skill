# DesignForge site (starter)

Buildable Next.js + Tailwind base that DesignForge fills in section by section.

    npm install
    npm run dev     # local preview
    npm run build   # production build

Deploy to Vercel / Cloudflare / any static host once built.
